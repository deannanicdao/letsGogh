<h1>Editing Item</h1>

<%= render 'form', item: @item %>

<%= link_to 'Show', @item %> |
<%= link_to 'Delete Listing', @item, method: :delete, data: { confirm: 'Are you sure?' } %> |
<%= link_to 'Back', items_path %>
